let valores = [2,7,9,5,4,3]
/*for (let pos =0; pos< valores.length; pos++) {console.log(`a posiçao ${pos} tem o valor ${valores[pos]} `)} */

/*for (let pos in valores) {console.log(`a posiçao${pos} tem o valor${valores[pos]}`)*/
/* for (let pos = 0; pos <valores.length; pos ++) {console.log(`a posiçao ${pos} tem ${valores[pos]} `)}*/
for (var pos in valores) {console.log(`a posiçao ${pos} tem esses valores ${valores[pos]}`)
/*console.log(`a ordem e a seguinte${valores.push(0)}`)*/}